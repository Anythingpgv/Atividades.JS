let base = prompt("insira a base")
let expoente = prompt("Insira o expoente")

function potencia(base, expoente) {
    return base ** expoente}
let resultado = potencia(base, expoente)
alert(`${base} elevado a ${expoente} é igual a ${resultado}`)